<?php
    //o php tem algumas variáveis globais

    //contém algumas informações sobre o servidor, php, ip que acessou o site etc;
    print_r($_SERVER);

    /*
        $_GET -> pega informações que foram enviadas via get (na url)
        $_POST -> pega informações que foram enviadas via post (internamente)
    */
?>